import React from 'react'
import ReactDOM from 'react-dom/client'
// Mude a linha abaixo para o nome EXATO do seu arquivo, MAS SEM o ".tsx" no final
import App from './terminal_industrial_guarda_soberana'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)

import React from 'react'
import ReactDOM from 'react-dom/client'
// Importando o nome EXATO do arquivo em inglês, com a extensão .tsx
import App from './terminal_industrial_sovereignguard.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)

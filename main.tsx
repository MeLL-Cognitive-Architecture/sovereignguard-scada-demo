import React from 'react'
import ReactDOM from 'react-dom/client'
// Importa o seu componente (Ajuste o nome do arquivo abaixo se necessário)
import App from './terminal_industrial_guarda_soberana.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
